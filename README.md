#This project uses body-parser and axios ensure it is downloaded by running the following lines of code
npm install -s axios
npm install -s body-parser

#To build
--

#Ref - https://github.com/ecojuntak/api-gateway/blob/master/routers/router.js